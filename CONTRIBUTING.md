# مشارکت در گرامر کلنگ (kolang-grammar)

این مخزن منبع حقیقت گرامر برجسته‌سازی نحو زبان کلنگ است. همهٔ تغییرهای گرامر باید از همین‌جا انجام شود و سپس به مخازن مصرف‌کننده همگام‌سازی شود.

## افزودن یا ویرایش کلیدواژه/دستور

1. **واژهٔ کلیدی** (کنترل، اعلان، copula، همروندی، فعل امری، literal، خود/والد): در مخزن مفسر کلنگ به `internal/token/token.go` اضافه/ویرایش کنید؛ `keywords.json` آنجا بازتولید می‌شود (منبع حقیقت lexer). اینجا چیزی دستی ویرایش نمی‌شود — مولد آینه را به‌روز می‌کند.
2. **توکن غیرکلیدواژه** (builtin، type، module، exception، عملگر، نشانه‌گذاری): فایل `kolang.grammar.json` را ویرایش کنید:
   - `builtins` برای توابع و نوع‌های داخلی
   - `types` برای نوع‌های داده
   - `exceptions` برای کلاس‌های خطا
   - `modules` برای ماژول‌ها
   - `operators` (بلندترین‌ها اول) و `punctuation`
3. واژه‌های ترکیبی را با **نیم‌فاصلهٔ دقیق** (`U+200C`) بنویسید — مثل `خطای‌صفر` و `بسته‌است`. نسخهٔ بدون نیم‌فاصله اگر در مفسر باشد، هر دو نگه داشته می‌شوند (مثل `حذف‌کن` و `حذفکن`).
4. بازتولید و همگام‌سازی:

```bash
npm run generate
npm run sync:vscode
```

5. صحت خروجی‌ها را بررسی کنید (JSON معتبر باشد، JS بدون خطای syntax) و سپس commit کنید.

## قواعد

- **هیچ‌وقت** خروجی‌های تولیدشده (`textmate/`، `codemirror/`) یا فایل‌های مخازن مصرف‌کننده را مستقیم ویرایش نکنید — همیشه `npm run generate` و سپس sync.
- **واژه‌های کلیدی را از `keywords.json` مفسر نگیرید و آنجا کپی نکنید**؛ مولد به‌صورت خودکار از روی `keywords.json` (مخزن همسایه) تولید می‌کند و هر «درِفت» (واژهٔ موجود در گرامر که مفسر نمی‌شناسد) را حذف و گزارش می‌دهد.
- **هیچ واژه‌ای حذف نکنید**؛ اگر واژه‌ای در مفسر باشد، باید در خروجی‌ها هم باشد.
- متن فارسی را با نیم‌فاصلهٔ درست و اعداد فارسی بنویسید.
- ورودی‌ها را در هر دسته مرتب و خوانا نگه دارید.
- عملگرها در آرایهٔ `operators` باید به ترتیب «بلندترین اول» نوشته شوند (مولد هنگام تولید مرتب می‌کند، ولی برای خوانایی همین ترتیب را نگه دارید).

## راه‌اندازی

فقط به `node` و `bash` نیاز دارید (برای مولد و اسکریپت‌های همگام‌سازی). `keywords.json` باید در مسیر همسایهٔ `../kolang/keywords.json` موجود باشد (یا با `KOLANG_KEYWORDS_JSON` مشخص شود). هیچ وابستگی دیگری وجود ندارد.

## گزارش اشکال و پیشنهاد

برای گزارش مشکل یا پیشنهاد ویژگی، یک issue در همین مخزن باز کنید.