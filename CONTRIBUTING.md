# مشارکت در گرامر کلنگ (kolang-grammar)

این مخزن منبع حقیقت گرامر برجسته‌سازی نحو زبان کلنگ است. همهٔ تغییرهای گرامر باید از همین‌جا انجام شود و سپس به مخازن مصرف‌کننده همگام‌سازی شود.

## افزودن یا ویرایش کلیدواژه/دستور

1. فایل `kolang.grammar.json` را باز کنید.
2. واژه را به دستهٔ درست اضافه/ویرایش کنید:
   - `keywords` برای کلیدواژه‌ها (زیردسته‌های `control`، `declaration`، `copula`، `logical`، `scope`، `other`)
   - `verbs` برای فعل‌های امری
   - `builtins` برای توابع و نوع‌های داخلی
   - `types` برای نوع‌های داده
   - `literals` برای مقادیر ثابت
   - `exceptions` برای کلاس‌های خطا
   - `modules` برای ماژول‌ها
3. واژه‌های ترکیبی را با **نیم‌فاصلهٔ دقیق** (`U+200C`) بنویسید — مثل `خطای‌صفر` و `بسته‌است`. نسخهٔ بدون نیم‌فاصله اگر در گرامرهای پیشین وجود داشته، باید هر دو نگه داشته شوند (مثل `حذف‌کن` و `حذفکن`).
4. خروجی‌های فرمتی را بازتولید کنید:
   - `textmate/kolang.tmLanguage.json`
   - `codemirror/kolang-syntax.js`
5. همگام‌سازی کنید:

```bash
npm run sync:vscode
npm run sync:web
```

6. صحت خروجی‌ها را بررسی کنید (JSON معتبر باشد، JS بدون خطای syntax) و سپس commit کنید.

## قواعد

- **هیچ‌وقت** خروجی‌ها یا فایل‌های مخازن مصرف‌کننده را مستقیم ویرایش نکنید — همهٔ تغییرها از `kolang.grammar.json` می‌آید.
- **هیچ واژه‌ای حذف نکنید**؛ اگر واژه‌ای در گرامرهای پیشین (TextMate یا CodeMirror) وجود داشته، در اتحاد کانونیکال حفظ می‌شود.
- متن فارسی را با نیم‌فاصلهٔ درست و اعداد فارسی بنویسید.
- ورودی‌ها را در هر دسته مرتب و خوانا نگه دارید.
- عملگرها در آرایهٔ `operators` باید به ترتیب «بلندترین اول» نوشته شوند.

## راه‌اندازی

فقط به `bash` و `node` نیاز دارید (برای اسکریپت‌های همگام‌سازی). هیچ وابستگی دیگری وجود ندارد.

## گزارش اشکال و پیشنهاد

برای گزارش مشکل یا پیشنهاد ویژگی، یک issue در همین مخزن باز کنید.